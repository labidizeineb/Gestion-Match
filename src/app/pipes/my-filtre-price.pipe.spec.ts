import { MyFiltrePricePipe } from './my-filtre-price.pipe';

describe('MyFiltrePricePipe', () => {
  it('create an instance', () => {
    const pipe = new MyFiltrePricePipe();
    expect(pipe).toBeTruthy();
  });
});
